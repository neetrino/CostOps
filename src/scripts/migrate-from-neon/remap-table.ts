/**
 * Manual Neon project id → existing CostOps project slug.
 * Do not guess Degusto-style merges by name equality; add rows here only.
 */
export const MANUAL_NEON_PROJECT_REMAP: Readonly<Record<string, string>> = {
  // 'old-neon-project-id': 'existing-costops-slug',
};
